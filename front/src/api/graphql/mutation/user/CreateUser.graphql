#import "@graphql/fragments/user/_User.graphql"

mutation($payload: UserPayload!) {
  User {
    create(payload: $payload) {
      uid
      email
      firstname
      lastname
      isAdmin
    }
  }
}
